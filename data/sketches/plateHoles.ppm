// File:plateHoles.ppm
// Description:2D rectangle with four circles near the corners (inside the outline)
// Author: Tim Olson
// Date: 2025-09-06
// Confirmed: +1

 

Units(1[in]);  // Work in inches

// ---------------- Parameters ----------------
Input(W, H, R, E);

W = Parameter("Width (X)",   4.0, LINEAR, GreaterThan(0));  // rectangle X extent
H = Parameter("Height (Y)",  3.0, LINEAR, GreaterThan(0));  // rectangle Y extent
R = Parameter("Circle Radius", 0.25, LINEAR, GreaterThan(0));

// Offset of circle centers in from each edge (same X/Y).
// Keep centers at least R away from each edge so circles stay inside.
E = Parameter(
      "Edge Offset (from each side)",
      0.5,
      LINEAR,
      Interval(R + 0.01, min(W, H) - R - 0.01)
    );

// ---------------- Build the rectangle outline (closed) ----------------
rect2D = Polyline(
  Point(0, 0),
  Point(W, 0),
  Point(W, H),
  Point(0, H),
  Point(0, 0)     // close loop
);

// ---------------- Create the four corner circles ----------------
c1 = Circle(R);               // base circle at (0,0)
c1 = Move(c1,      E,      E, 0);        // bottom-left

c2 = Circle(R);
c2 = Move(c2,  W - E,      E, 0);        // bottom-right

c3 = Circle(R);
c3 = Move(c3,  W - E,  H - E, 0);        // top-right

c4 = Circle(R);
c4 = Move(c4,      E,  H - E, 0);        // top-left

// ---------------- Output all 2D entities ----------------
Output(rect2D, c1, c2, c3, c4);
