// ---------------------------------------------
// Rectangle.ppm
// Description: Creates a 2D rectangle with width, height, and base point
// Author : Tim Olson
// Date : Sept 5, 2025
// Notes : none
// ---------------------------------------------

Units(1[in]);                          // use inches; change to 1[mm] if desired

// Expose controls in the part's Properties dialog
Input(W, H, P0);

// ------------------------------
// Parameters
// ------------------------------
W  = Parameter("Width",  4, LINEAR, GreaterThan(0));   // rectangle width
H  = Parameter("Height", 2, LINEAR, GreaterThan(0));   // rectangle height
P0 = ParameterPoint(0, 0, 0);                          // placement point (lower-left)

// ------------------------------
// Geometry
// ------------------------------
// Base rectangle is created at the global origin (0,0)
// Then it is moved so its lower-left corner sits at P0
R0 = Rectangle(W, H);                                  // 2D rectangle in XY plane
R1 = Move(R0, PointX(P0), PointY(P0), PointZ(P0));     // place at P0

// Optional visual reference for insertion
pr = RefPoint(PointX(P0), PointY(P0), PointZ(P0));     // shows the base corner

// ------------------------------
// Output
// ------------------------------
Output(R1, pr);                                        // send geometry (and ref point) to drawing
